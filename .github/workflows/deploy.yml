name: Deploy to Cloudflare Workers

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy Worker with curl
        env:
          ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          SCRIPT_NAME: cf-vless-worker
        run: |
          # 创建一个 metadata 文件，声明入口点
          echo '{"main_module": "worker.js"}' > metadata.json

          curl -X PUT "https://api.cloudflare.com/client/v4/accounts/$ACCOUNT_ID/workers/scripts/$SCRIPT_NAME" \
            -H "Authorization: Bearer $API_TOKEN" \
            -F "metadata=@metadata.json;type=application/json" \
            -F "script=@dist/_worker.js;type=application/javascript+module"
