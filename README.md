# CF-VLESS-Worker

A Cloudflare Worker/Pages implementation for VLESS protocol.

## Deployment

Deploying to Cloudflare Workers:
1. Ensure a project exists in your Cloudflare dashboard with the name `project-pages`.
2. Push changes to the `main` branch to trigger the GitHub Action.
